package lab.cmd.parser.console;

import lab.cmd.CommandManager;
import lab.cmd.cmd.GlobalScale;
import lab.cmd.parser.Parser;

public interface GlobalCommandParser<T> extends Parser<GlobalScale, CommandManager, T> {
}
